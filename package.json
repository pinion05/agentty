{
  "name": "agentty",
  "version": "0.0.0",
  "private": true,
  "bin": {
    "agentty": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "pretest": "npm run build",
    "test": "vitest run",
    "test:e2e": "npm run build && vitest run tests/e2e.*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "execa": "^9.5.2",
    "tsup": "^8.4.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  }
}
